import React, { ReactElement } from "react";
import { useRouter } from "next/router";

export default function UpdateArticleEditor() {
    const router = useRouter();

    return <>내 글 수정 페이지 입니다.</>;
}
